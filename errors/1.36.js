Переменные
Всегда используйте ключевое слово var перед объялением переменной, это позволит избежать попадания переменных в глобальную область видимости
Пример
// плохо
    superPower = new SuperPower();
    // хорошо
    var superPower = new SuperPower();
